class Bird {
                        // no fly function is
};                      // declared

class FlyingBird: public Bird {
public:
  virtual void fly();
};

class Penguin: public Bird {
                       // no fly function is
                       // declared
};
